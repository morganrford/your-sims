<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/stylesheet.css" />
    <title>Your Sims Now</title>
    <link rel="icon" type="image/x-icon" href="/assets/plumbob.png" />
  </head>
  <body>
    <%- include('./partials/_navbar.ejs') %>
    <section class="content">
      <div>
        <h1>Welcome to Your Sims Now!</h1>
      </div>
      <div>
          <% users.forEach((user) => { %> <% user.sims.forEach((sim) => { %>
          <a href="/users/<%= user._id %>/<%= sim._id %>"><img src="<%= sim.photo %>" alt="<%= sim.name %>" width="400px"></a>
          <% }) %> <% }) %>
      </div>
    </section>
  </body>
  <footer>
    <p>© 2025 Your Sims Now</p>
  </footer>
</html>
